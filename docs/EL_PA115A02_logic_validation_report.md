# EL_PA115A02 PLM 로직 정합성 검증 보고서

## 1. 개요
- **PID**: `EL_PA115A02`
- **검증 일시**: 2026-08-24
- **대상 데이터**: `output_csv/el_pa115a02.csv` (총 122개 행)
- **엑셀 출품 파일**: `output_excel/el_pa115a02.xlsx`

---

## 2. 요약 검증 결과

| 검증 항목 | 결과 | 비고 |
|---|---|---|
| **SPEC 공백 & CON 존재** | **0건 (정상)** | 문법 오류 없음 |
| **SPEC 존재 & CON 공백 (`[확인 필요]`)** | **77건** | 특성코드만 정의되고 조건값이 빈 칸 |
| **KEY 공백 & VAL 존재** | **0건 (정상)** | 문법 오류 없음 |
| **KEY 존재 & VAL 공백 (`[확인 필요]`)** | **6건** | 라인 3, 4, 5, 6, 7, 8 |
| **완전 중복 행 (문법 오류)** | **1그룹 (16개 라인)** | 빈 행(Empty Line) 중복 |
| **SPEC+CON 조건 중복** | **3그룹 (6개 라인)** | 동일 조건에 따른 산출 분기 확인 필요 |

---

## 3. 세부 검증 내용

### 3.1. `[확인 필요]` - SPEC 존재 및 CON 공백 라인 (총 77건)
SPEC(특성코드)은 선언되어 있으나 CON(특성값)이 빈 칸으로 등록되어 있습니다. PLM 문법 지침에 따라 임의로 단정하지 않고 `[확인 필요]`로 명시합니다.

- **대표 라인 목록**:
  - **라인 100 ~ 101**: `SPEC4='EL_ECWRL'`, `SPEC5='EL_ECWGP'`, `SPEC6='EL_ECWTP'`, `SPEC7='EL_ETM'`, `SPEC8='EL_ECWSF'`, `SPEC9='EL_ECWCC'`
  - **라인 102 ~ 103**: `SPEC4='EL_ECWRL'`, `SPEC5='EL_ECWGP'`, `SPEC7='EL_ETM'`, `SPEC8='EL_ECWSF'`, `SPEC10='EL_ACD2'`
  - **라인 104 ~ 111**: `SPEC5='EL_ECWGP'`, `SPEC10='EL_ACD2'`
  - **라인 112 ~ 113**: `SPEC4='EL_ECWRL'`, `SPEC5='EL_ECWGP'`, `SPEC6='EL_ECWTP'`, `SPEC7='EL_ETM'`, `SPEC8='EL_ECWSF'`, `SPEC9='EL_ECWCC'`
  - **라인 114 ~ 117**: `SPEC3='EL_AMR'`, `SPEC5='EL_ECWGP'`, `SPEC6='EL_ECWTP'`, `SPEC9='EL_ECWCC'`, `SPEC10='EL_ACD2'`
  - **라인 118**: `SPEC4='EL_ECWRL'`, `SPEC5='EL_ECWGP'`, `SPEC6='EL_ECWTP'`, `SPEC7='EL_ETM'`, `SPEC8='EL_ECWSF'`, `SPEC9='EL_ECWCC'`, `SPEC10='EL_ACD2'`

### 3.2. `[확인 필요]` - KEY 존재 및 VAL 공백 라인 (총 6건)
결과 반영 대상(KEY)은 선언되었으나 입력될 값(VAL)이 공백 상태입니다.

- **해당 라인 목록**:
  - **라인 3**: `KEY2 = 'DWG_A115A02_CAR'`, `VAL2 = ''`
  - **라인 4**: `KEY1 = 'EL_PA115A02_1'`, `VAL1 = ''`
  - **라인 5**: `KEY2 = 'DWG_A115A02_CWT'`, `VAL2 = ''`
  - **라인 6**: `KEY1 = 'EL_PA115A02_2'`, `VAL1 = ''`
  - **라인 7**: `KEY2 = 'DWG_A115A02_CAR2'`, `VAL2 = ''`
  - **라인 8**: `KEY1 = 'EL_PA115A02_3'`, `VAL1 = ''`

### 3.3. 완전 중복 행 (문법 오류)
ADDR, REMARKS를 제외하고 SPEC, CON, KEY, VAL, GOTO 필드가 모두 동일한 중복 행입니다.

- **중복 그룹 1 (빈 행 중복)**:
  - **해당 라인 (NO)**: `1, 9, 10, 16, 23, 28, 69, 70, 71, 72, 75, 79, 83, 99, 119, 120` (총 16개 행)
  - **유형**: SPEC, CON, KEY, VAL, GOTO가 모두 비어있는 빈 행 중복.

### 3.4. SPEC+CON 조건 중복 그룹 (산출 비교 분석)
동일한 SPEC+CON 조건을 공유하나 결과(KEY/VAL/GOTO)가 상이하거나 점검이 필요한 그룹입니다.

- **유효 조건 중복 그룹 1** (라인 `22`, `78`):
  - **조건**: `SPEC1='EL_ABRAND'`, `CON1='NEX_MR'`, `SPEC2='EL_ASPSCD'`, `CON2='KC01'`
- **유효 조건 중복 그룹 2** (라인 `26`, `97`):
  - **조건**: `SPEC1='EL_ATYP'`, `CON1=',GTLX,GTLX_E,'`
- **유효 조건 중복 그룹 3** (라인 `27`, `98`):
  - **조건**: `SPEC1='EL_ATYP'`, `CON1=',GTSS,GTSS_E,'`

---

## 4. 결론 및 조치 권고사항
1. **완전 빈 행 제거**: 라인 `1, 9, 10, 16, 23, 28, 69, 70, 71, 72, 75, 79, 83, 99, 119, 120`의 불필요한 빈 행 정리 권장.
2. **VAL 누락 확인**: 라인 `3 ~ 8`의 `DWG_A115A02_CAR`, `EL_PA115A02_1` 등에 설정할 자재번호/수식값 누락 여부 확인.
3. **CON 미입력 확인**: 라인 `100 ~ 118`의 SPEC만 존재하고 CON이 비어있는 77개 항목에 대하여 의도된 전 조건 수용 인지, 특성값 지정 누락 인지 현업 확인 필요.
