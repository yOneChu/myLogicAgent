# 로직 전산화/변경 요청서 (전산화 요청 04773)

## 1. 대상 PID
* **수정 대상 PID**: `CAL_CEILING_PLATE_HOLE_DWG_CMT` (최신 버전 기준)
* **관련 기능**: CEILING PLATE HOLE 가공도 관련 안내 주석(`CEILING_PLATE_HOLE_DWG_CMT`) 생성 로직

---

## 2. 변경 유형 및 위치
* **변경 유형**: SIDE KICK PLATE 따냄 및 T-CABLE BRACKET HOLE 반영 WORKING DWG 안내 주석 출력을 위한 신규 행 추가
* **삽입 위치(ADDR)**: `CAL_CEILING_PLATE_HOLE_DWG_CMT` PID의 `MAIN3_COMMON` 라벨 하위 (기존 NO 206 라인 다음 신규 NO 207 라인 삽입 권장)

---

## 3. 적용 조건 (SPEC / CON)
* **기본 조건 (공통 출력 시)**:
  * `SPEC1`: `-` (공백)
  * `CON1`: `-` (공백)
* **(확인 필요 조건)** 특정 사양에 한해 출력되는 경우 (예: CA, JJ 조건 등):
  * `SPEC1`: `EL_ECCA` (또는 관련 특성코드)
  * `CON1`: [확인 필요 조건값]

---

## 4. 산출 결과 및 출력 내용 (KEY / VAL)
* **산출 1**:
  * `KEY1`: `L_CMT` (신규 주석 문구 설정)
  * `VAL1`: `- CEILING PLATE WORKING DWG. 32116116 참고하여 제작할 것.(따냄 및 T-CABLE BRKT HOLE 반영 시)`
* **산출 2**:
  * `KEY2`: `CEILING_PLATE_HOLE_DWG_CMT` (기존 주석에 누적 이어붙이기)
  * `VAL2`: `{CEILING_PLATE_HOLE_DWG_CMT}{L_CMT}\n`

---

## 5. 참고 사항 (수식 및 변수 정의)
* **참조 도면**: CEILING PLATE WORKING DWG. `32116116`
* **관련 부품 및 간섭 요인**:
  * `CEILING ASSY`상 `CA`(`EL_ECCA`), `JJ`(`EL_ECJJ` 등) 값에 따라 `SIDE KICK PLATE` 따냄과 `T-CABLE BRACKET HOLE` 간섭 발생대응
* **PLM 특성코드 매핑 예시**:
  * `{CA}` -> `EL_ECCA`
  * `{JJ}` -> `EL_ECJJ` (필요시 참조)

---

## 6. 적용 사유
* **요청 번호**: 전산화 요청 `04773` (담당자: 김영환)
* **요청 목적**:
  * CEILING ASSY 상 CA, JJ 값에 따라 SIDE KICK PLATE 따냄과 T-CABLE BRACKET HOLE 간섭이 발생하는 현장에 대해, 제작 시 WORKING DWG 32116116을 참고하도록 안내 주석을 자동 출력함.

---

## 7. ⚠️ 추가 확인 및 결정이 필요한 정보 (Checklist)

실제 PLM 로직 반영 전 확정이 필요한 세부 정보 목록입니다.

1. **주석 출력 적용 조건(SPEC / CON) 확정**:
   * 본 주석 문구가 `CAL_CEILING_PLATE_HOLE_DWG_CMT` 로직이 호출될 때 **무조건(공통) 출력**되어야 하는지, 혹은 **특정 CA/JJ 조건이나 특정 도면/옵션 사양**일 때만 선택적으로 출력되어야 하는지 확인이 필요합니다.
2. **삽입 위치(GOTO 및 ADDR) 확정**:
   * `MAIN3_COMMON` 하단의 NO 207 위치에 단순 추가하여 분기 없이 통과하는 방식이 맞는지 검토가 필요합니다.
