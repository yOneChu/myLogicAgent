# 로직 전산화/변경 요청서 (전산화 요청 04672)

## 1. 대상 PID
* **수정 대상 PID**: `BOM_E321A`, `BOM_E321A16`, `BOM_E321A20`, `BOM_E321A26`, `BOM_E321A30`
* **참고용 PID**: `BOM_E321A` (기존 GTSS 및 GTSS-M, GTSS-D 분기 라인 참조)

---

## 2. 변경 유형 및 위치
* **변경 유형**: GTSS-M / GTSS-D CAR SUB FRAME 사양 추가 (DUTY 54차 반영)에 따른 사양 분기 및 CALL PID 연동 신규 행 추가
* **삽입 위치(ADDR)**: `BOM_E321A` PID의 `MAIN` 분기 구간 (기존 GTSS 관련 라인 NO 257~264, NO 459~464 영역 이후 하위 PID CALL 연동 구간)

---

## 3. 적용 조건 (SPEC / CON)
* **조건 1 (CAR SUB FRAME 사양구분)**:
  * `SPEC1`: `EL_ECSF` (CAR SUB FRAME 사양 특성코드)
  * `CON1`: `,GTSS-M,GTSS-D,` (또는 `,GTSS-M,` / `,GTSS-D,`)
* **조건 2 (BEAM 사양구분)**:
  * `SPEC2`: `EL_ECBE`
  * `CON2`: `,B255,B305,` / `,B260,B310,` / `,B270,B320,` / `,B280,B330,` / `,RGS100,` / `,RGS125,` 등

---

## 4. 산출 결과 및 출력 내용 (KEY / VAL)
* **산출 1 (하위 자재/로직 PID 호출)**:
  * `KEY1`: `CALL`
  * `VAL1`: `BOM_E321A16` (BEAM B255, B305, RGS100, RGS125 사양 시)
  * `VAL1`: `BOM_E321A20` (BEAM B260, B310 사양 시)
  * `VAL1`: `BOM_E321A26` (BEAM B270, B320 사양 시)
  * `VAL1`: `BOM_E321A30` (BEAM B280, B330 사양 시)
* **산출 2 (주석 및 비고 기록)**:
  * `KEY2`: `L_CMT` / `CMT` (필요 시 GTSS-M/GTSS-D 전산화 안내 주석 세트 반영)
  * `REMARKS`: `GTSS-M / GTSS-D CAR SUB FRAME 사양추가 (DUTY 54차 반영, 요청번호 04672)`

---

## 5. 참고 사항 (수식 및 변수 정의)
* **사양 및 특성코드 매핑**:
  * `{CAR SUB FRAME}` -> `EL_ECSF`
  * `{BEAM TYPE}` -> `EL_ECBE`
* **하위 호출 대상 PID 구조**:
  * `BOM_E321A` 실행 중 `EL_ECSF`가 `GTSS-M`, `GTSS-D`로 선택될 때 `EL_ECBE` 사양 조합에 따라 `BOM_E321A16`, `BOM_E321A20`, `BOM_E321A26`, `BOM_E321A30` PID를 호출하여 각 서브 프레임 자재를 세부 산출함.

---

## 6. 적용 사유
* **요청 번호**: 전산화 요청 `04672` (등록일: 2026-07-27 14:20:18, 등록자: 김동하, 담당자: 김동하, 상태: RLS)
* **제목**: `GTSS-M / GTSS-D CAR SUB FRAME 사양추가 (DUTY 54차 반영)`
* **목적**: DUTY 54차 개정에 따른 GTSS-M 및 GTSS-D 신규 CAR SUB FRAME 사양을 수배 로직에 추가 반영하여 자동 수배 및 주석 생성이 수행되도록 함.

---

## 7. ⚠️ 추가 확인 및 결정이 필요한 정보 (Checklist)
1. **각 하위 PID (`BOM_E321A16`, `BOM_E321A20`, `BOM_E321A26`, `BOM_E321A30`) 내 세부 자재 번호 반영 여부**:
   * 본 요청(04672)으로 상위 PID `BOM_E321A`에서 하위 PID 분기 CALL 설정 외에, 각 하위 PID 내부의 자재 규격 및 사양 분기 조건도 변경할 필요가 있는지 확인이 필요합니다.
2. **주석 신규 생성 (`L_CMT`) 항목 추가 여부**:
   * GTSS-M / GTSS-D 수배 시 도면 및 특성 체크 안내를 위한 별도의 `L_CMT` 주석 문구가 필요한지 최종 확정이 필요합니다.
